[project]
name = "pbs-efficientsam"
version = "0.1.0"
description = "EfficientSAM (Meta, CVPR 2024) - SAMI masked-image-pretraining, lightweight SAM"
requires-python = ">=3.10,<3.11"
dependencies = [
    "numpy<2",
    "torch==2.13.0",
    "torchvision",
    "opencv-python-headless>=4.6",
    "pillow>=9",
    "matplotlib>=3.5",
]

[tool.uv]
package = false
