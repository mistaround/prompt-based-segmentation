# Shared pins so all four uv environments resolve to the SAME torch build.
# uv hardlinks from a single cache, so one physical copy is shared across the venvs.
TORCH_VERSION=2.13.0
PYTHON_VERSION=3.10
